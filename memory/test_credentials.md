# Test Credentials

## BranchIQ
- No authentication. The app opens directly to the Executive Dashboard.
- Backend base: REACT_APP_BACKEND_URL + `/api`
- Key endpoint: `POST /api/consultant/ask` (LLM-backed via Emergent LLM key, Claude Sonnet, with rule-based fallback)
